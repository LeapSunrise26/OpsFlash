# Changelog

本项目的所有重要变更都将记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)（SemVer）规范。

## [Unreleased]

### 新增
- （待发布内容将在此累积）

## [0.5.0] - 2026-09-07

### 新增

- **指令编排（运维操作）**：从命令库 / 脚本库选取命令与脚本，按序组合成任务一次批量执行；步骤可自由排序、命令与脚本混排，执行中可随时停止
- **失败策略**：任务可配置「遇错停止」（某步失败立即中止，后续步骤自动跳过）或「忽略错误」（失败继续执行至完成），实现步骤间依赖前序成功的顺序编排
- **执行观测**：任务执行时逐条实时展示各步骤状态、输出、退出码与耗时，直至全部完成
- **流程参数**：执行前可填写参数，脚本步骤内以 `{{key}}` 占位符注入（如 `--version {{version}}`），非字符串值自动序列化
- **数据库连接走隧道**：Redis / MySQL / TDengine 连接可绑定本地 SSH 隧道，执行与连通性测试自动经隧道访问

### 变更

- 侧边栏菜单调整为 9 项，原「指令库」更名「命令库」，首页新增快捷入口（运维操作 / 命令库 / 连接管理）
- 删除被命令引用的连接将阻止并提示，删除隧道时自动解除关联连接的隧道配置
- 守护进程类型指令支持 Unix 平台；脚本库「全部」视图展示脚本所属环境

## [0.4.1] - 2026-09-01

### 修复

- **SSH 非交互指令执行后不结束**：命令衍生的后台进程持有 PTY 时 sshd 不发 EOF，`Wait()` 永久阻塞导致一直显示「运行中」；结束判定改为「Wait 返回 或 输出静默 10s」
- **SSH 流式收尾死锁**：`x/crypto/ssh` 不关闭 stdout writer 导致读取协程永久阻塞、会话无法结束；关闭会话时显式关闭输出管道
- **SSH 交互式 / 守护进程类型指令启动报「CMD 命令不支持整体多行脚本」**：该校验改为仅本地 terminal 模式生效
- **交互式会话停止后读取协程残留**：随输出管道关闭一并修复

### 新增

- **SSH 远程守护进程（后台类型指令）**：`nohup` 远程启动 + `kill -0` 查活 + `kill` 停止，支持 SSH 连接的守护进程类型指令

## [0.4.0] - 2026-08-31

### 新增

- **数据库执行（Redis / MySQL / TDengine）**：连接管理新增三种数据库类型（默认端口 6379 / 3306 / 6041，Redis DB 索引、MySQL / TDengine 库名字段），创建 / 编辑弹窗内一键测试连通性；指令库执行模式新增 redis / mysql / tdengine，目标连接下拉自动过滤匹配类型的连接
- **Redis 执行器**：基于 go-redis 执行任意指令（如 PING、GET、SET），单值 / 数组结果统一展开为行展示
- **MySQL 执行器**：基于 go-sql-driver 执行 SQL，SELECT / SHOW / DESC / EXPLAIN / WITH 查询类语句返回结果表格，其他语句返回影响行数；UTF-8 编码、连接超时兜底
- **TDengine 执行器**：通过 taosAdapter REST 接口（6041 端口）执行时序 SQL，纯 Go net/http 实现免 CGO；兼容 Taosd / Basic 两种认证前缀（401 自动重试），用户名密码冒号校验
- **结果表格渲染**：数据库查询结果以结构化表格（列头 + 行）展示在终端面板，非查询语句显示影响行数提示
- 数据库模式仅支持非交互式执行（流式 / 交互式 / 守护进程类型自动提示不可用）

## [0.3.0] - 2026-08-25

### 新增

- **指令库**：命令统一管理（增删改查、环境分组、搜索筛选、排序）；命令归属环境复用环境管理（与脚本库同源）；执行模式覆盖 **本地 terminal（cmd / PowerShell / Bash）** 与 **SSH 远程** 两种
- **命令执行引擎**：三种命令类型——非交互（同步执行 + 流式实时输出可停止，cmd 多行自动逐行执行）、交互（xterm 真实终端，支持 sudo / ssh 等密码交互，300ms 轮询增量输出）、守护进程（后台常驻、可启停、Windows Job Object 进程树管理，应用退出自动清理）
- **SSH 远程执行**：非交互流式（远程 PTY 整段执行、实时输出可停止）、交互式（远程 PTY 双向通道）；凭据认证复用连接管理（密码 / 私钥 / 口令）
- **终端体验**：xterm.js 渲染 + pty:output 事件流实时输出；ConPTY 启动噪声清洗（跨帧剥离清屏 / 填屏 / 光标定位序列）；多行会话式逐行执行（同一 shell 变量共享、哨兵判定行完成）；终端尺寸自适应同步

## [0.2.0] - 2026-08-18

### 新增

- **环境管理**：环境增删改查（名称 / 英文 key / 排序），英文 key 作为脚本磁盘子目录名 `data/scripts/{env_key}/`；删除环境时脚本自动迁移到目标环境（磁盘目录同步移动）；修改环境 key 自动迁移脚本目录；至少保留一个环境；脚本根目录的遗留平铺文件自动归入首个环境并迁入子目录
- **脚本库管理**：支持 bat / ps1 / sh 三种脚本类型；按环境隔离存储（`data/scripts/{环境key}/`）；脚本增删改查、跨环境迁移；脚本内容编辑器（Ctrl+S 快捷保存、未保存切换提醒）；磁盘文件与 DB 元数据自动同步（手动放入文件自动补录、文件丢失自动清理 DB 记录）
- **脚本执行引擎**：基于 PTY（ConPTY）实时输出，同脚本防重入；多会话并行执行（Tab 切换查看）；xterm.js 终端渲染，支持 ANSI 控制码；自定义参数传入；轮询增量获取输出，支持中途停止；退出码、耗时、错误信息完整呈现；sh 脚本 CRLF 自动转 LF 临时副本执行（不修改源文件）

## [0.1.0] - 2026-08-12

首个公开发布版本。基于 Wails v3 + Go + Vue 3 的跨平台桌面运维工具，单二进制分发，本地 SQLite 存储，敏感凭据加密保护。

### 新增

- **用户与认证**：登录 / 登出，Token 会话（24h 过期），密码 bcrypt 哈希，首次启动内置管理员账号
- **用户管理**：用户增删改查、重置密码
- **连接管理**：SSH 连接统一管理，支持密码 / 私钥 / 口令认证，凭据本地加密存储（Windows DPAPI / Unix AES-256-GCM）
- **SSH 隧道**：local / remote / dynamic 三种端口转发；一键启停；断线自动重连（指数退避）；应用启动自动拉起；隧道分组与批量创建
- **隧道观测**：环形日志面板、收发流量统计、实时运行状态
- **桌面体验**：系统托盘（关闭最小化）、每日滚动日志文件、深色主题界面

